# Pass Manager runtime dependencies — Python 3.10+
# Install:  pip install --user -r requirements.txt   then   python3 main.py
# (or just:  ./run.sh)

# --- GUI ---
PySide6>=6.8

# --- Cryptography ---
cryptography>=44.0          # AES-256-GCM, HKDF
argon2-cffi>=23.1.0         # Argon2id KDF (recovery path)

# --- System keyring (Secret Service: GNOME Keyring / KWallet) ---
SecretStorage>=3.3          # freedesktop Secret Service D-Bus client;
                            # if no daemon is running the app falls back to a
                            # local key file with an on-screen notice.

# --- Optional features ---
pyotp>=2.10.0                # TOTP / 2FA code generation
