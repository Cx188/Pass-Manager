"""Presentation layer.

Modules:
    window       frameless main window + custom draggable title bar
    theme        palette tokens, QSS, font loading
    transitions  QPropertyAnimation fade/slide screen changes
    widgets/     reusable components (buttons, cards, panels, meters, modals)
    screens/     setup, lock, dashboard, add_wizard, detail
"""
